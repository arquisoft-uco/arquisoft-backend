dependencies {
    implementation project(':shared:domain')
    implementation project(':shared:exceptions')

    // Spring Data Redis (incluye Jedis transitivamente, version gestionada por el BOM)
    implementation "org.springframework.boot:spring-boot-starter-data-redis"

    // Redis client (Jedis) — version gestionada por el BOM de Spring Boot 4.0.5
    implementation "redis.clients:jedis"

    // Serialization — Jackson 3.x viene transitivo de spring-boot-starter-data-redis
}
# El código, los recursos y catalogo/*.properties están en UTF-8, igual que el CI (Linux).
# Sin -Dfile.encoding=UTF-8 el daemon de Gradle en Windows arranca en Cp1252 y las llamadas
# a String.getBytes()/new String(byte[]) de los tests corrompen el JSON acentuado de los payloads.
# El resto de flags son los valores por defecto de Gradle 9 (declararlos aquí los fija explícitos).
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=384m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

javaVersion=21
springBootVersion=4.0.5
jUnitVersion=6.0.3
lombokVersion=1.18.36

# ==================== PLUGIN VERSIONS ====================
openapiVersion=1.9.0
springManagement=1.1.7

# ==================== DRIVER / LIBRARY VERSIONS ====================
# Solo declaramos aqui versiones que difieren del BOM de Spring Boot 4.0.5 o que el BOM no gestiona.
# Spring Boot 4.0.5 BOM ya gestiona: hibernate-validator (9.1.0.Final), jakarta.validation-api (3.1.1),
# h2 (2.4.240), flyway-core (12.4.0), jedis (gestionado), jackson-bom (3.1.2).
# Por eso esas versiones NO se declaran aqui — heredamos del BOM.
postgresVersion=42.7.2
minioVersion=8.5.12
# Bucket4j 8.x: grupo oficial Maven Central com.bucket4j. El artefacto bucket4j_jdk17-core
# requiere Java 17+ y es compatible con Java 21 y Virtual Threads (no usa synchronized internamente).
# Para futuras versiones LTS de Java (25+), solo actualizar esta versión.
bucket4jVersion=8.18.0
springdocVersion=2.8.8
# Spring Modulith: alineado con Spring Boot 4.x / Spring Framework 7.x.
# Verificar la versión exacta publicada para Spring Boot 4.0.5 en
# https://spring.io/projects/spring-modulith antes de compilar.
springModulithVersion=2.0.0

# keycloakVersion removido: el proyecto usa OAuth2 Resource Server nativo de Spring Security
# + HTTP directo al token endpoint (RestTemplate). No se requiere keycloak-admin-client ni starter.

# ==================== JACKSON 2/3 COEXISTENCE ====================
# Spring Boot 4.0.5 usa Jackson 3 (tools.jackson.*) para serialización HTTP.
# springdoc-openapi-starter-webmvc-ui:2.8.8 aún depende de Jackson 2 (com.fasterxml.jackson.*).
# Ambas Jackson coexisten sin conflicto por namespaces Java distintos.
# Revisar cuando springdoc publique una versión compatible con Jackson 3.
# Nota: logstash-logback-encoder fue eliminado — el perfil prod usa StructuredLogEncoder
# nativo de Spring Boot 4.x, que no tiene dependencias de Jackson 2.